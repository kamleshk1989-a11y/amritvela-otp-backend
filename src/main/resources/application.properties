spring.application.name=backend

# Railway will provide PORT automatically
server.port=${PORT:8080}

# 2Factor key comes from environment variable (do not hardcode)
twofactor.apiKey=${TWOFACTOR_API_KEY}

# Firebase service account JSON comes from environment variable
firebase.serviceAccountJson=${FIREBASE_SERVICE_ACCOUNT_JSON:}
